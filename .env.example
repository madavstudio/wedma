# Server-only. Never use a VITE_ prefix for these settings.
RESEND_API_KEY=
# An address on a domain verified in Resend, for example WEDMA <website@your-domain>
CONTACT_FROM=
# Public origin in production, including scheme and optional port, without trailing slash.
CONTACT_ORIGIN=
HOST=127.0.0.1
PORT=4173
